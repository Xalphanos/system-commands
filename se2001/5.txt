## script.awk

# Compute the average of the first column and print a word based on a 0.5 threshold.

{
    sum += $1
}
END {
    if (sum / NR > 0.5)
        print "not"
    else
        print "naught"
}
